;; Get character from ASCII Value
;; Write a function getChar/GetChar/get_char which takes a number and returns the corresponding ASCII char for that value.

(ns kata)

(defn get_char [number]
  (char number))